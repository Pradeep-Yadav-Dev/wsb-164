/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
    remotePatterns: [new URL('https://wscubetech.co/Assignments/furniture/storage/**')],
  },
};



export default nextConfig;
